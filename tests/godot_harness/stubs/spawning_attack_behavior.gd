extends Reference

# Stub of Brotato's SpawningAttackBehavior.
# Used only for `is SpawningAttackBehavior` type checks in enemy_force.gd.
# class_name registers this as a global type identifier in GDScript parser.

class_name SpawningAttackBehavior
