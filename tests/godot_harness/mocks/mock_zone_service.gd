extends Node

# AutoLoad singleton mock of Brotato's ZoneService.
# Holds arena boundary metadata. Mod reads `current_zone_max_position`.

var current_zone_max_position: Vector2 = Vector2(1920, 1080)
